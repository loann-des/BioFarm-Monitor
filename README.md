# BioFarm-Monitor
Bio Farm monitoring 

Installation :
python -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install requirements.txt

setting :
    export FLASK_APP = app.py

init :
    flask init_db
    flask run

